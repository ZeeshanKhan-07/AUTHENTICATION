server:
    port: 8080

#database connection:

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/authentication?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
        username: root
        password: SQL@0718
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect
        show-sql: true

logging:
    level:
        org.springframework.security: DEBUG